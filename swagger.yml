openapi: '3.0.2'
info:
  title: argos-repository-cache
  version: '1.0'
servers:
  - url: http://localhost
paths:
  /repositories/garbagemza/repo/tag:
    get:
      responses:
        '200':
          description: OK
    post:
      responses:
        '200':
          description: OK
